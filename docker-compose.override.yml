services:
  postgres:
    ports:
      - "5433:5432"

  # backend / celery / frontend run natively on the host during dev
  backend:
    profiles: ["full"]
  celery-worker:
    profiles: ["full"]
  frontend:
    profiles: ["full"]
